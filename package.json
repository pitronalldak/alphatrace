{
  "name": "alphatrace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "prisma:pull": "prisma db pull",
    "prisma:generate": "prisma generate",
    "db:update": "npm run prisma:pull && npm run prisma:generate",
    "prisma:studio": "prisma studio",
    "env:init": "node -e \"const fs=require('fs'); if(!fs.existsSync('.env')) { fs.copyFileSync('.env.example','.env'); console.log('Created .env from .env.example'); } else { console.log('.env already exists'); }\"",
    "env:init:local": "node -e \"const fs=require('fs'); if(!fs.existsSync('.env.local')) { fs.copyFileSync('.env.example','.env.local'); console.log('Created .env.local from .env.example'); } else { console.log('.env.local already exists'); }\"",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "next": "15.4.6",
    "@prisma/client": "^5"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "prisma": "^5"
  }
}
